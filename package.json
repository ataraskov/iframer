{
  "name": "iframer",
  "version": "1.0.0",
  "description": "Reverse proxy that lets you embed sites which block iframing (X-Frame-Options / CSP frame-ancestors) inside your own iframe.",
  "private": true,
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.19.2"
  }
}
